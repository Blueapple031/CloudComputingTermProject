# DDoS 부하 실험 및 오토스케일링 관측 실습 환경 — 개발 계획서

## 1. 프로젝트 개요

### 1.1 배경 및 목적

클라우드 환경에서 DDoS에 따른 부하 변화, 로드밸런싱, 오토스케일링, 요요(Yo-Yo) 현상을 직접 관측·측정할 수 있는 격리된 실습 환경을 구축한다.

본 프로젝트는 팀이 소유한 클라우드 리소스 내부에서만 제어된 트래픽을 발생시키는 교육·실험 목적의 시스템이다. 외부 서비스 공격 기능은 구현하지 않는다.

### 1.2 핵심 목표

| 목표 | 설명 |
|------|------|
| 부하 가시화 | 공격 트래픽 발생 시 더미 웹 서버의 CPU, 메모리, 응답 시간, 처리량(RPS) 변화를 실시간 확인 |
| 탄력성 검증 | 로드밸런서 + 오토스케일링 적용 전/후의 장애 임계점과 회복 시간 비교 |
| 요요 현상 관측 | 스케일 아웃/인 임계값 설정에 따른 인스턴스 수 진동(oscillation) 패턴 기록 |
| 보고서 근거 확보 | 실험 시나리오, 원시 지표, 그래프, 분석 결과를 재현 가능하게 수집 |

### 1.3 기대 산출물

- **EC2 #1 — Attacker Web**: 부하 생성 및 실험 제어 UI/API
- **EC2 #2 — Dummy Web**: 부하를 받는 타깃 서비스
- **확장 인프라 (후속 단계)**: 로드밸런서, 오토스케일링 그룹, 모니터링 대시보드
- **실험 스크립트/시나리오 문서**: 재현 가능한 실험 절차
- **최종 보고서**: 실험 결과, 그래프, 분석, 개선안

---

## 2. 시스템 아키텍처

### 2.1 EC2 2대, 서비스 1:1 배치

| EC2 | 서비스 | 역할 |
|-----|--------|------|
| EC2 #1 | attacker-web | 부하 생성, 실험 제어 UI |
| EC2 #2 | dummy-web | 부하 수신, 메트릭 노출 |

Attacker EC2 → Dummy EC2로 HTTP 요청. 한 EC2에 두 서비스를 올리지 않는다.

### 2.2 기본 구성도 (Phase 1 — EC2 2대)

```
┌──────────────────────────────────────────────────────────────┐
│                    격리된 VPC (실습 전용)                       │
│                                                              │
│   ┌─────────────────────────┐    HTTP 부하    ┌────────────┐ │
│   │  EC2 #1 — Attacker      │ ──────────────► │ EC2 #2 —   │ │
│   │                         │   (Private IP   │ Dummy      │ │
│   │  attacker-web           │    또는 VPC     │            │ │
│   │  - RPS / 패턴 설정 UI   │    내부 통신)    │ dummy-web  │ │
│   │  - 실험 시작/중지       │                 │ - /health  │ │
│   │  - 실험 로그            │                 │ - /api/load│ │
│   └─────────────────────────┘                 └─────┬──────┘ │
│                                                     │        │
│                                                     ▼        │
│                              ┌──────────────────────────┐   │
│                              │  CloudWatch (양쪽 EC2)    │   │
│                              │  CPU, Network, 응답 시간  │   │
│                              └──────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

**관측:** RPS 증가 시 Dummy EC2 CPU·응답 시간 변화, 임계 RPS·5xx 발생 시점

### 2.3 확장 구성도 (Phase 2 — LB / ASG 추가)

로드밸런싱·스케일링·요요 현상 실험은 Dummy EC2 앞단에 ALB + ASG를 추가한다. Attacker EC2는 1대 유지.

```
┌──────────────────────────────────────────────────────────────────┐
│                       격리된 VPC (실습 전용)                       │
│                                                                  │
│  ┌──────────────────────┐         ┌───────────────────────────┐  │
│  │  EC2 #1 — Attacker   │  HTTP   │  Application Load         │  │
│  │  attacker-web        │ ──────► │  Balancer (ALB)           │  │
│  └──────────────────────┘  부하    └────────────┬──────────────┘  │
│                                                 │ 분산             │
│                                    ┌────────────┼────────────┐    │
│                                    ▼            ▼            ▼    │
│                              ┌─────────┐ ┌─────────┐ ┌─────────┐ │
│                              │ EC2 #2  │ │ EC2 #3  │ │ EC2 #N  │ │
│                              │ dummy   │ │ dummy   │ │ dummy   │ │
│                              │ -web    │ │ -web    │ │ -web    │ │
│                              └────┬────┘ └────┬────┘ └────┬────┘ │
│                                   └───────────┼───────────┘      │
│                                               ▼                  │
│                              ┌────────────────────────────┐     │
│                              │  Auto Scaling Group (ASG)   │     │
│                              │  Launch Template: dummy-web │     │
│                              │  min=1, max=5~10            │     │
│                              └────────────────────────────┘     │
│                                               │                  │
│                                               ▼                  │
│                              ┌────────────────────────────┐     │
│                              │  CloudWatch Dashboard       │     │
│                              └────────────────────────────┘     │
└──────────────────────────────────────────────────────────────────┘
```

> Phase 2: EC2 #2를 ASG 초기 인스턴스로 두고, scale-out 시 dummy-web EC2 추가.

### 2.4 EC2별 정의

#### EC2 #1 — attacker-web

| 항목 | 내용 |
|------|------|
| 기능 | RPS·지속 시간 설정, 실험 시작/중지 |
| 대상 URL | Phase 1: Dummy EC2 IP / Phase 2: ALB DNS |
| SG | Outbound → Dummy(또는 ALB)만 허용 |

#### EC2 #2 — dummy-web

| 항목 | 내용 |
|------|------|
| 엔드포인트 | `GET /health`, `GET /api/load`, `GET /api/db-sim` |
| SG | Inbound → Attacker SG(Phase 1) 또는 ALB SG(Phase 2)만 허용 |

### 2.5 인프라 컴포넌트

#### Phase 1 (필수 — EC2 2대)

| 컴포넌트 | 역할 |
|----------|------|
| VPC + Subnet | Attacker EC2, Dummy EC2 격리 배치 |
| EC2 #1 | attacker-web 호스팅 |
| EC2 #2 | dummy-web 호스팅 |
| Security Group × 2 | Attacker → Dummy 단방향 통신만 허용 |
| CloudWatch | 양 EC2 CPU, NetworkIn/Out, StatusCheck |

#### Phase 2 (확장 — LB/ASG)

| 컴포넌트 | 역할 |
|----------|------|
| ALB | Dummy Web EC2들 앞단 트래픽 분산 |
| ASG + Launch Template | dummy-web AMI 기반 EC2 자동 증감 |
| Scaling Policy | CPU/요청 수 기반 Scale Out/In |
| CloudWatch Dashboard | 인스턴스 수, LB 지표, 요요 현상 시각화 |
| S3 (선택) | 실험 로그 및 스냅샷 저장 |

---

## 3. 기술 스택 제안

### 3.1 권장 구성 (AWS 기준)

| 영역 | 기술 |
|------|------|
| 클라우드 | AWS EC2 × 2 (Phase 1), + ALB/ASG (Phase 2) |
| EC2 #1 | attacker-web (Python FastAPI + asyncio/aiohttp) |
| EC2 #2 | dummy-web (Python FastAPI) |
| OS | Amazon Linux 2023 또는 Ubuntu 22.04 |
| 배포 | Docker + ECR + GitHub Actions |
| IaC | Terraform (선택) |
| 모니터링 | CloudWatch |

### 3.2 EC2 스펙 제안

| EC2 | 인스턴스 타입 (예시) | 비고 |
|-----|---------------------|------|
| Attacker | t3.small | 부하 생성은 CPU/네트워크 위주 |
| Dummy Target | t3.micro ~ t3.small | 임계점 실험을 위해 타입 통일 권장 |

> Phase 2 ASG로 dummy-web EC2가 추가될 때 동일 Launch Template(동일 타입) 사용.

### 3.3 로컬 개발

- `deploy/docker-compose.yml`로 attacker-web + dummy-web 동시 기동
- EC2 배포 전 Phase 1 흐름을 로컬에서 먼저 확인

---

## 4. 실험 시나리오 설계

### 4.1 Phase 1 — EC2 2대 직접 연결 실험

| # | 시나리오 | 구성 | 목적 | 주요 관측 지표 |
|---|----------|------|------|----------------|
| E1 | 베이스라인 | Attacker EC2 → Dummy EC2 (직접) | 단일 타깃 임계 RPS 측정 | Dummy EC2 CPU, 응답 시간, 5xx 발생 시점 |
| E2 | 지속 부하 | 동일 | 장시간 부하 시 CPU/메모리 추이 | CPU plateau, 응답 시간 drift |
| E3 | Step 부하 | RPS를 단계적으로 증가 (100→500→1000…) | 임계 구간 pinpoint | RPS vs p95 latency 곡선 |

### 4.2 Phase 2 — LB / ASG 확장 실험

| # | 시나리오 | 구성 | 목적 | 주요 관측 지표 |
|---|----------|------|------|----------------|
| E4 | LB만 적용 | Attacker EC2 → ALB → Dummy EC2 (ASG OFF) | LB 경유 오버헤드 확인 | ALB TargetResponseTime |
| E5 | ASG Scale Out | ASG min=1, max=5 | 스케일 아웃 반응 속도 | 트리거~InService 시간, CPU 회복 |
| E6 | ASG Scale In 포함 | Scale In 정책 활성화 | **요요 현상** 유발·관측 | 인스턴스 수 진동 패턴 |
| E7 | 정책 튜닝 | Cooldown / Target Tracking 변경 | 요요 완화 | 인스턴스 수 진동 감소 |

### 4.3 요요(Yo-Yo) 현상 관측 방법 (Phase 2 — E6)

요요 현상은 **스케일 아웃과 스케일 인 임계값 차이가 작거나, cooldown이 짧을 때** CPU가 임계값 근처에서 반복적으로 오르락내리락하며 인스턴스 수가 불안정하게 변하는 현상이다.

**의도적 유발 설정 예시 (실험 E6):**

```
Scale Out: CPU 평균 60% 초과 → +1 인스턴스
Scale In:  CPU 평균 50% 미만 → -1 인스턴스
Cooldown:  60초 (짧게 설정)
부하:      RPS를 60%↔55% 경계에서 변동 (step 또는 sine 패턴)
```

**기록할 그래프:**

1. 시간축 vs ASG Desired Capacity (인스턴스 수)
2. 시간축 vs 평균 CPU Utilization
3. 시간축 vs ALB Target Response Time (p50, p95)
4. 시간축 vs Request Count per Target

---

## 5. 개발 범위 및 기능 명세

### 5.1 Attacker Web — 기능 목록

| ID | 기능 | 우선순위 |
|----|------|----------|
| A-01 | 대상 URL 설정 (Phase 1: Dummy EC2 IP / Phase 2: ALB DNS) | P0 |
| A-02 | RPS 슬라이더 (10 ~ 10,000) | P0 |
| A-03 | 실험 지속 시간 설정 (초/분) | P0 |
| A-04 | Ramp-up / Ramp-down 패턴 | P1 |
| A-05 | Step 부하 (갑작스런 RPS 변경) | P0 |
| A-06 | 실험 시작/중지 및 상태 표시 | P0 |
| A-07 | 실험 ID별 로그 저장 (JSON/CSV) | P1 |
| A-08 | 웹 대시보드 (현재 RPS, 누적 요청 수) | P1 |

### 5.2 Dummy Web — 기능 목록

| ID | 기능 | 우선순위 |
|----|------|----------|
| D-01 | `GET /health` — 200 OK, 경량 | P0 |
| D-02 | `GET /api/load?ms=100` — CPU/지연 부하 | P0 |
| D-03 | 인스턴스 ID / hostname 응답 헤더 | P0 |
| D-04 | CloudWatch Agent 연동 | P0 |
| D-05 | `GET /` — 간단한 HTML 상태 페이지 | P2 |

### 5.3 인프라 — 기능 목록

#### Phase 1 (EC2 2대)

| ID | 기능 | 우선순위 |
|----|------|----------|
| I-01 | VPC + Subnet (Single AZ) | P0 |
| I-02 | EC2 #1 생성 + attacker-web 배포 | P0 |
| I-03 | EC2 #2 생성 + dummy-web 배포 | P0 |
| I-04 | SG: Attacker → Dummy 포트만 허용 | P0 |
| I-05 | CloudWatch CPU/Network 모니터링 (양 EC2) | P0 |
| I-06 | Attacker EC2 SSH / UI 접근 제한 | P0 |

#### Phase 2 (LB / ASG 확장)

| ID | 기능 | 우선순위 |
|----|------|----------|
| I-07 | ALB + Target Group (Dummy EC2 등록) | P0 |
| I-08 | Launch Template (dummy-web AMI) | P0 |
| I-09 | ASG (min=1, max=5~10) | P0 |
| I-10 | Scaling Policy (Target Tracking / Step) | P0 |
| I-11 | Attacker 대상 URL → ALB DNS로 전환 | P0 |
| I-12 | CloudWatch Dashboard | P0 |

---

## 6. 모니터링 및 데이터 수집

### 6.1 수집 지표

| 카테고리 | 지표 | 대상 |
|----------|------|------|
| EC2 (Attacker) | CPUUtilization, NetworkOut | EC2 #1 |
| EC2 (Dummy) | CPUUtilization, NetworkIn, StatusCheckFailed | EC2 #2 |
| LB (Phase 2) | RequestCount, TargetResponseTime, HTTPCode_Target_5XX | ALB |
| ASG (Phase 2) | GroupDesiredCapacity, GroupInServiceInstances | ASG |
| 애플리케이션 | RPS, Error Rate, p50/p95 Latency | attacker-web 로그 + CloudWatch |

### 6.2 대시보드

- CPU, 인스턴스 수, 응답 시간, RPS (실험 시작/종료 시점 표시)

### 6.3 데이터 Export

- CloudWatch 스크린샷, 실험별 CSV (타임스탬프, RPS, CPU, instance count)

---

## 7. 개발 일정 (8주 예시)

| 주차 | 단계 | 작업 내용 | 산출물 |
|------|------|-----------|--------|
| 1주 | 기획·설계 | EC2 2대 아키텍처 확정, VPC/SG 설계 | 설계 문서, 네트워크 다이어그램 |
| 2주 | Dummy Web | dummy-web 개발, CI 워크플로 초안, EC2 #2 배포 | dummy-web 코드, deploy-dummy.yml |
| 3주 | Attacker Web | attacker-web 개발, EC2 #1 배포, path-filter CI/CD 연동 | attacker-web 코드, deploy-attacker.yml |
| 4주 | Phase 1 실험 | E1~E3 베이스라인 실험, CloudWatch 수집 | 실험 raw data #1, CPU/RPS 그래프 |
| 5주 | Phase 2 인프라 | ALB, ASG, Launch Template 구성 | IaC 코드, Attacker → ALB 전환 |
| 6주 | Phase 2 실험 1 | E4~E5 LB·Scale Out 실험 | 실험 raw data #2 |
| 7주 | Phase 2 실험 2 | E6~E7 요요 현상 유발·완화 | yo-yo 그래프, 분석 초안 |
| 8주 | 보고서 | 결과 분석, PPT/보고서 작성, 데모 준비 | 최종 보고서, 발표 자료 |

---

## 8. 레포 구조 · Docker 배포 · CI/CD

한 레포에서 두 서비스를 관리하고, 변경된 서비스만 해당 EC2에 배포한다.

### 8.1 Docker vs GitHub Actions — 역할 구분

둘은 대체 관계가 아니다.

| | Docker | GitHub Actions |
|---|--------|----------------|
| 역할 | 앱 실행 환경 패키징 (EC2에서 컨테이너로 실행) | push 시 빌드·배포 자동화 |
| 실행 위치 | EC2 (및 로컬) | GitHub 서버 |

```
push → Actions(빌드·ECR push·EC2 배포) → EC2(docker pull & up)
```

**Docker를 쓰는 이유 (이 프로젝트 기준):**
- 로컬 / EC2 환경 차이 줄이기
- attacker-web, dummy-web을 이미지로 분리
- Phase 2 ASG scale-out 시 동일 이미지 pull만으로 신규 인스턴스 기동

**Actions가 하는 일:** `services/dummy-web/**` 변경 시 EC2 #2만 배포, 테스트·ECR push·SSH 배포 자동 실행.

### 8.2 디렉터리 구조

```
CloudComputingTermProject/
├── .github/workflows/
│   ├── ci.yml                 # PR: test + docker build
│   ├── deploy-attacker.yml    # attacker-web 변경 → EC2 #1
│   └── deploy-dummy.yml       # dummy-web 변경 → EC2 #2
├── services/
│   ├── attacker-web/
│   │   ├── app/
│   │   ├── deploy/
│   │   │   ├── deploy.sh
│   │   │   └── docker-compose.prod.yml
│   │   ├── Dockerfile
│   │   └── requirements.txt
│   └── dummy-web/             # 구조 동일 → EC2 #2
├── deploy/
│   └── docker-compose.yml     # 로컬 개발용
├── infra/
│   └── scripts/bootstrap-ec2.sh   # Docker 설치 등 최초 1회
├── docs/
├── experiments/
└── README.md
```

### 8.3 Docker 배포 흐름

**EC2 최초 1회 (`bootstrap-ec2.sh`):**
- Docker, Docker Compose, AWS CLI 설치
- `/opt/app/{attacker-web,dummy-web}/`에 deploy 스크립트·compose 파일 배치

**배포 1회 (`deploy.sh`):**
```bash
aws ecr get-login-password | docker login ...
docker compose -f docker-compose.prod.yml pull
docker compose -f docker-compose.prod.yml up -d
curl -f http://localhost:<port>/health
```

**GitHub Actions (`deploy-dummy.yml` 등):**
1. `services/<name>/` 변경 감지 (`paths` 필터)
2. Docker build → **ECR push**
3. SSH로 해당 EC2 접속 → `deploy.sh` 실행

**ECR 리포지토리:** `attacker-web`, `dummy-web` 각 1개.

**Phase 2 ASG:** Launch Template user-data에서 dummy-web 이미지 pull + compose up (EC2 #2와 동일 방식).

### 8.4 GitHub Secrets

| Secret | 용도 |
|--------|------|
| `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` | CI → ECR push |
| `ATTACKER_EC2_HOST` / `DUMMY_EC2_HOST` | 배포 대상 EC2 |
| `EC2_SSH_PRIVATE_KEY` / `EC2_SSH_USER` | SSH 배포 |
| `TARGET_URL` | attacker-web 부하 대상 (Dummy IP / ALB DNS) |

### 8.5 .gitignore

`.env`, `*.pem`, `experiments/results/`, `terraform.tfvars`

---

## 9. 보안 및 윤리적 운영 원칙

1. **격리**: VPC 내 EC2 2대 배치, SG로 Attacker EC2 → Dummy EC2(또는 ALB)만 허용
2. **접근 통제**: Attacker Web UI는 팀원 IP 또는 SSH 터널로만 접근
3. **대상 제한**: attacker-web에서 타깃 URL을 화이트리스트(Dummy EC2 IP 또는 ALB DNS)로 제한
4. **비용 상한**: EC2 2대 상시 + ASG max capacity 제한, Budget Alert 설정
5. **실험 종료**: Phase 2 ASG scale-in, 불필요 EC2/EIP 즉시 해제
6. **법적 고지**: 본 시스템은 교육 목적이며, 무단 외부 공격에 사용 금지

---

## 10. 리스크 및 대응

| 리스크 | 영향 | 대응 |
|--------|------|------|
| AWS 비용 초과 | 높음 | EC2 2대 t3.small 이하, ASG max=5, 실험 시간 제한 |
| Attacker가 Dummy EC2 직접 우회 | 낮음 | Phase 2에서 Dummy SG는 ALB SG만 inbound 허용 |
| Attacker EC2에서 과도한 outbound | 중간 | RPS 상한 설정, attacker-web 내부 rate limit |
| CI/CD 배포 실패 | 중간 | deploy.sh 헬스체크, 실패 시 이전 이미지 tag로 rollback |
| 요요 현상 재현 실패 | 중간 | Scale Out/In 임계값 차이를 좁히고 cooldown 짧게 설정 |

---

## 11. 보고서 가이드

1. 서론 — DDoS와 클라우드 탄력성
2. 실험 환경 — EC2 2대, 스케일링 정책
3. 실험 방법 — E1~E7 시나리오
4. 결과 — 그래프 (임계 RPS, scale-out 시간, yo-yo 패턴)
5. 분석·결론 — LB/ASG 효과, 한계(비용, Single AZ 등)

**분석 질문 예시:** 단일 dummy-web 임계 RPS는? ASG 적용 후 처리량 변화는? yo-yo가 발생한 조건은?

---

## 12. MVP

**Phase 1 (3~4주):** dummy-web + attacker-web, Docker 배포, E1 실험  
**Phase 2 (8주):** ALB + ASG, E6 yo-yo 실험, 보고서

---

## 13. 다음 액션

1. 레포 골격 + Dockerfile 작성
2. EC2 2대 bootstrap, ECR·GitHub Secrets 등록
3. dummy-web → EC2 #2, attacker-web → EC2 #1 배포
4. E1 베이스라인 실험 → Phase 2 확장

---

*문서 버전: v1.3*

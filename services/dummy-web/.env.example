# dummy-web 로컬/EC2 환경 변수 예시
# 복사: cp .env.example .env

# EC2 metadata 또는 수동 지정 (LB 실험 시 응답 헤더로 확인)
INSTANCE_ID=local-dev

# /api/load 기본값 (ms)
DEFAULT_LOAD_MS=100
MAX_LOAD_MS=5000

DEFAULT_DB_DELAY_MS=50
MAX_DB_DELAY_MS=3000

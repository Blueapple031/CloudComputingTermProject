graph TD
    %% 외부 영역
    User[👨‍💻 도환 (Admin)]
    GitHub[🐙 GitHub Actions<br/>(CI/CD Pipeline)]

    subgraph AWS Cloud [☁️ AWS Cloud]
        ECR[(📦 Amazon ECR<br/>attacker-web / dummy-web)]
        CW[(📊 CloudWatch<br/>1분 세부 모니터링)]

        subgraph VPC [🛡️ 격리된 VPC (10.0.0.0/16)]
            
            subgraph Public_Subnet [🌐 Public Subnets]
                Attacker[⚔️ Attacker EC2<br/>t3.small<br/>(Push 배포 / 고정 IP)]
                ALB[⚖️ Application Load Balancer<br/>(Deregistration: 10s)]
            end
            
            subgraph Private_Subnet [🔒 Private Subnets]
                ASG((🔄 Auto Scaling Group<br/>Min: 1, Max: 5<br/>CPU Target: 50~60%))
                Dummy1[🎯 Dummy EC2 #1<br/>t3.micro (Pull 배포)]
                Dummy2[🎯 Dummy EC2 #2<br/>t3.micro (Pull 배포)]
                DummyN[🎯 Dummy EC2 #N<br/>t3.micro (Pull 배포)]
                
                ASG -.- Dummy1
                ASG -.- Dummy2
                ASG -.- DummyN
            end
        end
    end

    %% CI/CD 흐름 (Push vs Pull)
    GitHub -. "1. Docker 빌드 & ECR Push" .-> ECR
    GitHub -. "2. SSH 접속 (Push 배포)" .-> Attacker
    Dummy1 -. "3. 부팅 시 자동 Pull (User Data)" .-> ECR

    %% 관리자 흐름
    User == "실험 제어 UI (8080)" ==> Attacker

    %% 트래픽 흐름 (부하 공격)
    Attacker == "HTTP 부하 (최대 2000 RPS)<br/>Connection: 2000" ==> ALB
    ALB == "트래픽 분산 (8000)" ==> Dummy1
    ALB == "트래픽 분산 (8000)" ==> Dummy2
    ALB == "트래픽 분산 (8000)" ==> DummyN

    %% 모니터링 흐름
    Dummy1 -. "CPU 지표 전송" .-> CW
    CW -. "CPU 임계치 초과 시 스케일링 트리거" .-> ASG